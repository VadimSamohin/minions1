var x = require ("loader");
var _ = require ("underscore");
var igetp = toString(loader.pi);
//after you must upload "igetp", divide it to array and with help of passfrase fin
//d needed file
//frim phone sorry for unconviniences
